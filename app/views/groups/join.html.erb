<turbo-frame id="modal">
  <div class="modal" data-controller="modal" data-action="keyup@document->modal#escClose">
    <button type="button" class="modal-backdrop" tabindex="-1" data-action="modal#close"></button>
    <div class="modal-window">
      <%= form_with model: @group, url: groups_add_path, method: :post, local: true, :html => {:class => "form"} do |f| %>
        <div class="form__titles">
          <span class="form__title">Rejoindre un <span class="mark">groupe</span> </span>
          <span class="form__subtitle">Classe, projet ou soutien, toutes les excuses sont bonnes pour se regrouper</span>
          <span class="form__arrow">↓</span>
        </div>
        <%= render(ErrorRenderComponent.new(object: @group)) %>
        <div class="form__field">
          <%= f.label "Clé d'accès", :class => "form__label" %>
          <%= f.text_field :key, autofocus: true, :class => "form__input", required: true %>
        </div>

        <div class="form__links">
          <%= f.submit "Rejoindre", :class => "form__submit", data: { turbo: "true" } %>
        </div>
      <% end %>
    </div>

  </div>
</turbo-frame>