<!DOCTYPE html>
<html>
  <head>
    <title>ObjectifLune</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
  <% if user_signed_in? %>
  <%= render(HeaderComponent.new(user: current_user)) %>
  <% end %>
    <%= yield %>
  </body>
</html>
